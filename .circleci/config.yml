version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          name: Setup environment
          command: |
            python -m venv venv
            . venv/bin/activate
            make bootstrap
            make test
